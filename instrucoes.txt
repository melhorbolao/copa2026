ALERTA CRÍTICO DE INFRAESTRUTURA: O projeto Supabase emitiu um aviso de "Disk IO Budget Depletion". Precisamos estancar o consumo excessivo de leitura/escrita no banco de dados. 

Execute um processo de raciocínio analítico detalhado (step-by-step) para identificar e corrigir os gargalos de performance. Não aplique correções impulsivas. Siga este roteiro:

1. AUDITORIA DE ÍNDICES (INDEXES) NO POSTGRESQL:
- Analise a estrutura das tabelas com maior volume de dados ('predictions', 'daily_rankings_snapshot', 'page_views', 'top_scorers').
- A falta de índices é a principal causa de Disk IO alto (Full Table Scans). 
- Elabore queries SQL para criar índices ('CREATE INDEX') em todas as colunas que são frequentemente usadas em cláusulas WHERE, JOIN e ORDER BY (especialmente chaves estrangeiras como 'user_id', 'match_id' e colunas de data como 'created_at').

2. OTIMIZAÇÃO DE LEITURAS (FRONTEND E CACHE):
- Faça uma varredura nos hooks de busca de dados no Next.js/React. 
- Verifique se a aplicação está fazendo fetch desnecessário a cada montagem de componente.
- Certifique-se de que o gerenciador de estado (SWR, React Query ou cache do Next App Router) está configurado com 'staleTime' ou 'dedupingInterval' adequados (ex: segurar os dados da tabela de classificação por pelo menos 30 a 60 segundos).

3. OTIMIZAÇÃO DE AGREGAÇÕES (VIEWS VS CLIENT-SIDE):
- Verifique a funcionalidade de "Zebras" e o "Comparador MB". O sistema está baixando todos os palpites do banco para calcular os percentuais no Javascript (sobrecarregando a rede e o banco)?
- Se sim, refatore para usar Views do Supabase (ex: 'vw_predictions_stats') que já entregam os percentuais agregados prontos.

4. THROTTLING DE ESCRITAS (TELEMETRIA):
- Se a tabela 'page_views' foi implementada, verifique se ela está disparando um INSERT a cada milissegundo que o usuário respira no site. Implemente um 'debounce' ou 'throttle' no registro de acessos.

5. VERIFICAÇÃO INDEPENDENTE:
- Antes de aplicar qualquer código ou executar comandos SQL no Supabase, PARE e me apresente:
  A) As queries de CREATE INDEX exatas que você planeja rodar.
  B) Um diagnóstico de qual componente frontend está fazendo mais requisições concorrentes (Problema N+1).
  C) A justificativa matemática de como essas mudanças reduzirão a carga de I/O.